{
  "name": "@mesreltime/notification-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo lint notification-service",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo test notification-service"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@mesreltime/shared-auth": "workspace:*",
    "@mesreltime/shared-config": "workspace:*",
    "@mesreltime/shared-logger": "workspace:*",
    "bullmq": "^5.21.2",
    "fastify": "^5.1.0",
    "mongoose": "^8.8.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
