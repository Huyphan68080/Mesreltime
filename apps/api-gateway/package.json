{
  "name": "@mesreltime/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "echo lint api-gateway",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo test api-gateway"
  },
  "dependencies": {
    "@fastify/cookie": "^10.0.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.1.0",
    "@fastify/sensible": "^5.6.0",
    "@mesreltime/shared-auth": "workspace:*",
    "@mesreltime/shared-config": "workspace:*",
    "@mesreltime/shared-logger": "workspace:*",
    "@mesreltime/shared-zod": "workspace:*",
    "fastify": "^5.1.0",
    "fastify-plugin": "^5.0.1",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
